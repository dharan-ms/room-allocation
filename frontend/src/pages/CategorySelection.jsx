import StudentDashboard from "./StudentDashboard";

// This page reuses StudentDashboard to keep flow simple and beginner-friendly.
const CategorySelection = () => {
  return <StudentDashboard />;
};

export default CategorySelection;
